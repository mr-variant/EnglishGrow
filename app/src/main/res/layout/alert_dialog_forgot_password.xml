<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:id="@+id/textViewTitleForgotPass"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/bg_login_green"
        android:paddingBottom="9dp"
        android:paddingTop="9dp"
        android:text="@string/forgotPassTitle"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/editTextRestoreEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:drawableLeft="@drawable/ic_mail"
        android:drawablePadding="4dp"
        android:hint="@string/fieldEmail"
        android:inputType="textEmailAddress" />

    <TextView
        android:id="@+id/textViewRestoreEmailError"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="4dp"
        android:text="@string/loginIsNull"
        android:textColor="@color/colorScarlet"
        android:textSize="8sp"
        android:visibility="invisible" />


</LinearLayout>
